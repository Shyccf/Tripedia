import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#43a047",
      light: "#e8f5e9",
      contrastText: "#fafafa",
    },
    secondary: {
      main: "#f9a825",
    },
    background: {
      default: "#fafafa",
    },
  },
});


